[project]
name = "ssalsa"
version = "0.0.1-alpha"
description = "Incrementalized computation for Python"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["ssalsa"]
